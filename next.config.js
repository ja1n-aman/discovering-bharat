/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.pexels.com",
      "cdn.pixabay.com",
      "www.pexels.com"
    ],
  },
};

module.exports = nextConfig;
